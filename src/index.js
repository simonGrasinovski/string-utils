const reverse = require('./reverse');
const countVowels = require('./countVowels');
const countConsonants = require('./countConsonants');
const uniqueCharacters = require('./uniqueCharacters');
const camelCase = require('./camelCase');
const titleCase = require('./titleCase');
const snakeCase = require('./snakeCase');
const kebabCase = require('./kebabCase');
const countOccurrences = require('./countOccurrences');
const extractNumber = require('./extractNumber');
const longestWord = require('./longestWord');
const shortestWord = require('./shortestWord');
const truncate = require('./truncate');
const randomCharacters = require('./randomCharacters');
const substringBetween = require('./substringBetween');
const removeConsecutiveSpaces = require('./removeConsecutiveSpaces');
const isValidURL = require('./isValidURL');
const isValidHex = require('./isValidHex');
const isValidDate = require('./isValidDate');
const isValidEmail = require('./isValidEmail');

module.exports = {
  reverse,
  countVowels,
  countConsonants,
  uniqueCharacters,
  camelCase,
  titleCase,
  snakeCase,
  kebabCase,
  countOccurrences,
  extractNumber,
  longestWord,
  shortestWord,
  truncate,
  randomCharacters,
  substringBetween,
  removeConsecutiveSpaces,
  isValidURL,
  isValidHex,
  isValidDate,
  isValidEmail,
};
